findAbi <- function(pressLA, pressRA, pressLL, pressRL) {
  pslao = findpressurearm(pressLA);
  psrao = findpressurearm(pressRA);
  psllo = findpressureleg(pressLL);
  psrlo = findpressureleg(pressRL);
}